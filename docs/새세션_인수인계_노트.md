# 🎮 게임빌더_v1 — 새 세션 인수인계 노트

> **작성일**: 2026-07-20 11:22 KST
> **최신 YML**: v44 (오디오→이미지 순서 교체 완료)
> **이미지**: heroine1~10 전체 등록 완료 (535개)

---

## 0. 지금 어디까지 왔나 (TL;DR)

- Dify advanced-chat + Supabase + Cloudflare R2/Worker 기반 텍스트 게임
- **최신 워크플로우**: `게임빌더_v44.yml` (72노드/75엣지)
- **TTS 인라인 재생**: `/음성` 또는 auto_tts ON 시 서사 직후 음성 → 이미지
- **이미지 시스템**: image_library 535개, heroine1~10 전체 등록, default fallback 정상
- **남은 과제**: R2 mp3 라이프사이클, 반복대사 dedupe, Fish Audio 과금 (2026-07-31)

---

## 1. 접근 정보 (전부)

| 서비스 | URL/키 | 용도 |
|---|---|---|
| **Dify 앱** | https://udify.app/chat/XCBQyQ9sd4yko8ke | 대화 |
| **Dify API** | `app-hSLkVFdwYtq6vyW1JynISnN9` | 테스트 |
| **Supabase** | https://ckzwlmoojtmcpwlqsqzv.supabase.co | 게임 DB |
| **Supabase Key** | `sb_secret_NkwcIvqMYFA1ybDh_cUH0w_9TdimbO0` | API |
| **GitHub** | zeroslove-ai/py-all | YML/문서 저장 |
| **GitHub Token** | `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` | API |
| **Cloudflare Worker** | fancy-dust-7f8c | TTS 중계 |
| **Fish API** | `c9d1c2d7cf1e4e78891ac5f07bf80e18` | 음성 생성 |

---

## 2. PY 환경 사용법 (Kimi = Python 실행기)

```python
import requests, json, yaml, base64, hashlib

# Supabase 예시
supabase_url = "https://ckzwlmoojtmcpwlqsqzv.supabase.co"
service_key = "sb_secret_NkwcIvqMYFA1ybDh_cUH0w_9TdimbO0"
headers = {
    "apikey": service_key,
    "Authorization": f"Bearer {service_key}",
    "Content-Type": "application/json"
}

# GET
r = requests.get(f"{supabase_url}/rest/v1/image_library?limit=10", headers=headers)
print(r.json())

# GitHub GraphQL (REST 503 시 우회)
token = "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"
headers_gql = {"Authorization": f"Bearer {token}", "Content-Type": "application/json"}
```

**⚠️ Storage 조회 시 주의**: `heroineX/sex/`는 별도 prefix로 재조회해야 함

---

## 3. 사용자 우선순위 (절대 규칙)

1. **스트리밍 속도 최우선** — 서사LLM-답변 사이 코드 노드 금지
2. **이미지 출력 정확성** — 이중 이미지 방지, 최종 렌더러가 한 장 붙임
3. **TTS 안정화** — auto_tts 기본 ON, 오디오→이미지 순서
4. **YML 버전 관리** — GitHub 영구 + Supabase 최신 5개, deliverable YML 필수
5. **보안/API키 이슈 제기 금지** — 사용자가 직접 키 공유하며 작업

---

## 4. 실수 방지 체크리스트

- [ ] Storage 파일명은 **실제 조회 후** 매칭 (사용자 키값 ≠ 실제 파일명)
- [ ] 하위 폴더(`sex/`)는 **별도 prefix**로 재조회
- [ ] YML 무결성: nodes/edges 수, dangling, 중복 반드시 검증
- [ ] Assigner 노드: `items` 배열 + `version: "2"` (variables 아님)
- [ ] if-else `case_id`: `"true"` (문자열)
- [ ] 새 변수는 `conversation_variables`에도 선언
- [ ] `app.name` = 파일명과 동일 (`게임빌더_v44`)

---

## 5. 이미지 시스템 현황

| 캐릭터 | 일반 | 성인 | default | 총 |
|---|---|---|---|---|
| heroine1~4 | 기존 | 기존 | 기존 | 333개 |
| heroine5 | 2 | 30 | 2 | 34개 |
| heroine6 | 10 | 20 | 1 | 31개 |
| heroine7 | 10 | 22 | 1 | 33개 |
| heroine8 | 13 | 20 | 1 | 34개 |
| heroine9 | 9 | 24 | 1 | 34개 |
| heroine10 | 10 | 25 | 1 | 36개 |
| **총** | **58** | **462** | **15** | **535개** |

---

## 6. YML 버전 히스토리 (최근)

| 버전 | 날짜 | 핵심 변경 |
|---|---|---|
| v40 | 07-19 | TTS 인라인, auto_tts, 구조 축소 |
| v43 | 07-20 | Claude rebuild, 코드파서 뒤 음성 브랜치 |
| **v44** | **07-20** | **오디오→이미지 순서 교체 (마이너)** |

---

## 7. 남은 과제

- [ ] R2 mp3 라이프사이클 (오래된 파일 자동삭제)
- [ ] 반복대사 dedupe (동일 대사 연속 방지)
- [ ] Fish Audio 과금 정책 (s2.1-pro-free 2026-07-31 종료)

---

*이 문서는 다음 세션 시작 시 업로드하면 Kimi가 즉시 상황 파악 가능*
